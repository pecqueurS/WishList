{% extends "main.twig" %}

	{% block stylesheetBase %}
		{{ parent() }}
		<link rel="stylesheet" href="css/views/home.css">
	{% endblock %}

	{% block header %}
		<h2>Connexion</h2>
		{{ parent() }}
	{% endblock %}
			

	{% block container %}
		<div id="description">
			<form method="POST" action="{{ href }}">
				{{ form('SignIn', 'login') }} 
				{{ form('SignIn', 'pwd') }} 
				<div id="form-btn">
					<a class="btn" href="/">
						<i class="fa fa-remove fa-fw"></i>
						<span>Annuler</span>
					</a>
					<button id="submit" class="btn" type="submit" name="submit"><i class="fa fa-sign-in fa-fw"></i> Se connecter</button>
				</div>
			</form>
		</div>
	{% endblock %}
